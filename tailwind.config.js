/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  // Tailwind v4 uses @theme in CSS for custom colors
  // See src/index.css for color definitions
  plugins: [],
}
